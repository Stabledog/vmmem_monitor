{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build vmmem_monitor.exe",
            "type": "shell",
            "command": "make",
            "problemMatcher": [
                "$msvc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
